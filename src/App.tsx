import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="welcome-card" aria-labelledby="welcome-title">
        <p className="eyebrow">Windows Desktop Console</p>
        <h1 id="welcome-title">Hello, OmniDeck!</h1>
        <p className="description">
          Your integration control center is ready to be built.
        </p>
      </section>
    </main>
  )
}

export default App
